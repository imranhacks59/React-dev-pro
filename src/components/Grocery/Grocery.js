import { Fragment } from "react"

const Grocery=()=>{

    return(
        <Fragment>
       <h1>This is grocery page. when we can build separate js  file using lazy loading.. </h1>
       <h2>like body also it can have multiple child component.</h2>
       </Fragment>
    )
}

export default Grocery